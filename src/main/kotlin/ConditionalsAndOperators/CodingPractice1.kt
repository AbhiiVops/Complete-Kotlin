package ConditionalsAndOperators

fun main() {

  //  Compare the length of two strings, a = “Internshala” and b = “Trainings” and print “length is same”
//  if it is same and print “Length is different” if it is different.

    val a = "Internshala"
    val b  = "Trainings"

    if(a.length == b.length)
        println("Length is same")
    else
        println("Length is not same")

}