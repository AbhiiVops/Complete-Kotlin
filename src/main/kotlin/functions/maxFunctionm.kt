fun main(){

    println(max(2,8))

}

fun max(a:Int,b:Int):Int {
    return maxOf(a,b)   // --> maxOf function is used to compare the two numbers and give
                        // the max of the two

}